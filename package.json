{
  "scripts": {
    "tailwind": "npx tailwindcss -i ./input.css -o ./src/style/main.css --watch",
    "browserSync": "browser-sync start --server --no-notify --directory --files \"src/**/*\" --start-path \"src/index.html\"",
    "dev": "concurrently --kill-others \"npm run browserSync\" \"npm run tailwind\"",
    "start": "npm run browserSync"
  },
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "concurrently": "^8.2.0",
    "tailwindcss": "^3.3.2"
  }
}
